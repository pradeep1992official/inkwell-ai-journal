rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Owner-bound isolation: Users can only read, upload, and delete files under their own userId path
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
